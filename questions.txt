1. `grz_denoise()`: current rule retains a new point when movement exceeds `radius_m` or elapsed time exceeds `max_gap_mins`. Confirm if this v1 denoise rule is acceptable.
2. `grz_clean_spatial()`: for daily modal paddock ties, current tie-break is alphabetical paddock name. Confirm preferred tie-break rule.
3. `grz_calculate_spatial()`: `kde95_ha` currently uses a covariance-ellipse proxy rather than a full KDE contour. Confirm whether to keep proxy in v1.
