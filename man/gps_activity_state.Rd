% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gps_activity.R
\name{gps_activity_state}
\alias{gps_activity_state}
\title{Classify GPS activity state}
\usage{
gps_activity_state(
  data,
  method = c("gmm_hmm", "gmm", "hmm"),
  groups = NULL,
  state_col = "activity_state",
  inactive_prob_col = "inactive_prob",
  feature_set = c("adaptive", "step_turn"),
  fit_max_rows = 200000L,
  seed = 1,
  verbose = TRUE,
  return_class = c("data.frame", "data.table"),
  ...
)
}
\arguments{
\item{data}{Input GPS data.}

\item{method}{Activity-state method. \code{"gmm_hmm"} is the default. \code{"gmm"} uses
mixture-model classification without HMM smoothing. \code{"hmm"} fits a direct
HMM.}

\item{groups}{Grouping columns used for track-wise features and decoding.}

\item{state_col}{Output state column.}

\item{inactive_prob_col}{Output inactive-probability column.}

\item{feature_set}{Feature set. \code{"adaptive"} augments step distance and turn
angle with rolling net displacement and straightness. \code{"step_turn"} uses
only step distance and turn angle.}

\item{fit_max_rows}{Maximum rows used for model fitting. Rows are sampled
when there are more valid rows than this value.}

\item{seed}{Random seed for reproducible fitting or subsampling.}

\item{verbose}{Logical; print summary output.}

\item{return_class}{Output class: \code{"data.frame"} (default) or \code{"data.table"}.}

\item{...}{Additional model arguments, such as \code{step_col}, \code{turn_col},
\code{adaptive_window_mins}, \code{max_iter}, \code{tol}, \code{min_var}, or
\code{hmm_self_transition}.}
}
\value{
Input data with activity-state columns appended. Model diagnostics
are stored in the \code{gps_activity_state} attribute.
}
\description{
Classifies GPS-derived movement state as \code{active} or \code{inactive}. The default
\code{gmm_hmm} method fits a two-component Gaussian mixture model to movement
features, then applies HMM smoothing to reduce short label flicker. The
\code{"gmm"} method uses the same mixture model without HMM smoothing. The \code{"hmm"}
method fits a direct two-state HMM.
}
