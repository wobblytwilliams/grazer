% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gps_movement.R
\name{gps_steps}
\alias{gps_steps}
\title{Calculate GPS step-level movement metrics}
\usage{
gps_steps(
  data,
  groups = NULL,
  verbose = TRUE,
  return_class = c("data.frame", "data.table")
)
}
\arguments{
\item{data}{Data frame with \code{sensor_id}, \code{datetime}, \code{lon}, \code{lat}.}

\item{groups}{Grouping columns for step calculations. Defaults to
\code{deployment_id} and \code{sensor_id} when \code{deployment_id} is present, otherwise
\code{sensor_id}.}

\item{verbose}{Logical; print a short summary.}

\item{return_class}{Output class: \code{"data.frame"} (default) or \code{"data.table"}.}
}
\value{
Data with appended \code{step_dt_s}, \code{step_m}, \code{speed_mps},
\code{bearing_deg}, \code{turn_rad}, \code{cum_distance_m}, and \code{net_displacement_m}
fields.
}
\description{
Builds row-level step metrics independently within each animal or sensor
stream. Coordinates are assumed to be WGS84 longitude and latitude in decimal
degrees. Step distances are great-circle haversine distances using a
spherical earth radius of 6,371,000 m.
}
