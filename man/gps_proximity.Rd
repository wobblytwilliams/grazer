% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gps_social.R
\name{gps_proximity}
\alias{gps_proximity}
\title{Calculate pairwise GPS proximity by timestamp}
\usage{
gps_proximity(
  data,
  herd_groups = NULL,
  interpolate = FALSE,
  align_interval_mins = "base",
  verbose = TRUE,
  return_class = c("data.frame", "data.table")
)
}
\arguments{
\item{data}{Data frame with \code{sensor_id}, \code{datetime}, \code{lon}, and \code{lat}.}

\item{herd_groups}{Herd partition columns. Defaults to available
\code{deployment_id} and \code{paddock}, so animals are only compared within those
partitions.}

\item{interpolate}{Logical; if \code{TRUE}, regularise and interpolate fixes
before comparing animals.}

\item{align_interval_mins}{Alignment interval in minutes, or \code{"base"} to use
the median observed interval.}

\item{verbose}{Logical; print a short summary.}

\item{return_class}{Output class: \code{"data.frame"} (default) or \code{"data.table"}.}
}
\value{
Pairwise proximity table with identifiers, \code{datetime}, \code{pair_id}, and
\code{distance_m}.
}
\description{
Returns one row for each unordered pair of fixes within the same timestamp
and herd partition. Distances are haversine distances in metres from WGS84
longitude and latitude. Pairwise output grows as \code{n * (n - 1) / 2} within
each timestamp, so large herds and fine sampling intervals can produce large
tables. For nearest-neighbour distances or range counts, use
\code{gps_nearest_neighbour()} or \code{gps_neighbours_within_range()} to avoid keeping
all pair rows in memory at once.
}
