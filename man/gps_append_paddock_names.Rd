% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gps_cleaning.R
\name{gps_append_paddock_names}
\alias{gps_append_paddock_names}
\title{Append paddock names to GPS fixes}
\usage{
gps_append_paddock_names(
  data,
  paddocks_sf,
  name_col = NULL,
  paddock_col = "assigned_paddock",
  buffer_m = 0,
  metric_crs = NULL,
  epoch = c("day", "hour"),
  epoch_mins = NULL,
  groups = NULL,
  min_prop = 0.7,
  min_fixes = 1L,
  verbose = TRUE,
  return_class = c("data.frame", "data.table")
)
}
\arguments{
\item{data}{Data frame with \code{sensor_id}, \code{datetime}, \code{lon}, and \code{lat}.}

\item{paddocks_sf}{\code{sf} polygon object containing paddock or area polygons.}

\item{name_col}{Column in \code{paddocks_sf} containing paddock names. If \code{NULL},
a complete \code{NAME}, \code{Name}, \code{name}, or \code{Description} column is selected.}

\item{paddock_col}{Output paddock column name.}

\item{buffer_m}{Optional buffer distance in metres. Use \code{0} for a direct
point-in-polygon overlay.}

\item{metric_crs}{Projected CRS used when \code{buffer_m > 0}. \code{NULL} selects a
UTM CRS from the GPS coordinates.}

\item{epoch}{Epoch level for assignment: \code{"day"} or \code{"hour"}. Supplying
\code{epoch_mins} uses fixed-duration intervals.}

\item{epoch_mins}{Optional fixed epoch duration in minutes.}

\item{groups}{Grouping columns used for animal or sensor tracks.}

\item{min_prop}{Minimum proportion of valid GPS fixes in an animal-epoch
that must fall in one paddock before it is assigned.}

\item{min_fixes}{Minimum valid GPS fixes required per animal-epoch.}

\item{verbose}{Logical; print a short summary.}

\item{return_class}{Output class: \code{"data.frame"} (default) or \code{"data.table"}.}
}
\value{
GPS data with an \code{assigned_paddock} column appended by default.
}
\description{
Assigns paddock or area names to GPS fixes using a point-in-polygon overlay.
GPS fixes are treated as WGS84 longitude and latitude. Paddock polygons must
have a CRS, or are assumed to be WGS84 when the CRS is missing.
}
