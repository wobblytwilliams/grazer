% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gps_movement.R
\name{gps_turning}
\alias{gps_turning}
\title{Calculate GPS turning angles}
\usage{
gps_turning(
  data,
  groups = NULL,
  unit = c("radians", "degrees", "both"),
  verbose = TRUE,
  return_class = c("data.frame", "data.table")
)
}
\arguments{
\item{data}{Data frame with \code{sensor_id}, \code{datetime}, \code{lon}, \code{lat}.}

\item{groups}{Grouping columns for step calculations. Defaults to
\code{deployment_id} and \code{sensor_id} when \code{deployment_id} is present, otherwise
\code{sensor_id}. When \code{segment_id} is present it is always used to prevent
steps across segment boundaries. \code{cum_distance_m} carries forward between
segments within the requested track groups without adding a gap step.}

\item{unit}{Turning angle unit to return. \code{"radians"} returns \code{turn_rad},
\code{"degrees"} returns \code{turn_deg}, and \code{"both"} returns both columns.}

\item{verbose}{Logical; print a short summary.}

\item{return_class}{Output class: \code{"data.frame"} (default) or \code{"data.table"}.}
}
\value{
Data with step bearings and turning angle fields.
}
\description{
Thin wrapper around \code{gps_steps()} for workflows that need bearing and turning
fields. Turning angles are absolute changes between consecutive step bearings
within each group.
}
