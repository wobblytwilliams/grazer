% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gps_movement.R
\name{gps_movement_summary}
\alias{gps_movement_summary}
\title{Summarise GPS movement by epoch}
\usage{
gps_movement_summary(
  data,
  epoch = c("day", "hour", "week", "month", "interval"),
  epoch_mins = NULL,
  groups = NULL,
  verbose = TRUE,
  return_class = c("data.frame", "data.table")
)
}
\arguments{
\item{data}{Data frame with GPS rows or output from \code{gps_steps()}.}

\item{epoch}{Epoch level: \code{"day"} (default), \code{"hour"}, \code{"week"}, \code{"month"},
or \code{"interval"}.}

\item{epoch_mins}{Positive epoch duration in minutes. Supplying this uses
fixed-duration \code{"interval"} epochs anchored to Unix time in UTC.}

\item{groups}{Grouping columns for step calculations. Defaults to
\code{deployment_id} and \code{sensor_id} when \code{deployment_id} is present, otherwise
\code{sensor_id}.}

\item{verbose}{Logical; print a short summary.}

\item{return_class}{Output class: \code{"data.frame"} (default) or \code{"data.table"}.}
}
\value{
Epoch summary table with movement metrics.
}
\description{
Summarises step-level movement within calendar or fixed-duration epochs.
Coordinates are assumed to be WGS84 longitude and latitude in decimal
degrees. Distances come from \code{gps_steps()}, which uses haversine distances on
a spherical earth. For epoch summaries, the first fix in each epoch has no
within-epoch step, so steps crossing an epoch boundary are not counted in
\code{total_distance_m} or speed summaries.
}
