% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gps_social.R
\name{gps_contacts}
\alias{gps_contacts}
\title{Calculate GPS contact events}
\usage{
gps_contacts(
  data,
  contact_distance_m,
  herd_groups = NULL,
  interpolate = FALSE,
  align_interval_mins = "base",
  max_gap_mins = NULL,
  min_duration_mins = 0,
  verbose = TRUE,
  return_class = c("data.frame", "data.table")
)
}
\arguments{
\item{data}{Data frame with \code{sensor_id}, \code{datetime}, \code{lon}, and \code{lat}.}

\item{contact_distance_m}{Contact threshold in metres.}

\item{herd_groups}{Herd partition columns. Defaults to available
\code{deployment_id} and \code{paddock}, so animals are only compared within those
partitions.}

\item{interpolate}{Logical; if \code{TRUE}, regularise and interpolate fixes
before comparing animals.}

\item{align_interval_mins}{Alignment interval in minutes, or \code{"base"} to use
the median observed interval.}

\item{max_gap_mins}{Maximum gap between contact fixes in the same event. If
\code{NULL}, the median positive timestamp interval in the pairwise data is used.}

\item{min_duration_mins}{Minimum event duration to retain.}

\item{verbose}{Logical; print a short summary.}

\item{return_class}{Output class: \code{"data.frame"} (default) or \code{"data.table"}.}
}
\value{
Contact event table with pair identifiers, event timing, duration,
contact-fix counts, distance summaries, and \code{contact_distance_m}.
}
\description{
Detects runs of pairwise proximity where distance is less than or equal to a
contact threshold. Events are built separately for each pair and herd
partition. A single contact fix has \code{duration_s = 0}. For irregular
timestamps, use \code{max_gap_mins} to decide how far apart contact fixes can be
while still belonging to the same event.
}
