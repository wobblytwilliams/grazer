% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gps_cleaning.R
\name{gps_clean}
\alias{gps_clean}
\title{Clean GPS data using a selected sequence of steps}
\usage{
gps_clean(
  data,
  steps = c("duplicates", "errors", "speed_fixed", "denoise"),
  action = c("drop", "flag"),
  paddocks_sf = NULL,
  max_speed_mps = 4,
  speed_stat_method = c("mad", "quantile"),
  window_start = NULL,
  window_end = NULL,
  deployment_windows = NULL,
  deployment_groups = NULL,
  buffer_m = 100,
  append_paddock = TRUE,
  paddock_col = "paddock",
  denoise_method = c("statistical", "state_aware"),
  denoise_state_col = NULL,
  denoise_inactive_states = c("inactive", "rest", "resting", "idle", "stationary",
    "lying", "ruminating"),
  denoise_keep_raw_coords = TRUE,
  step_args = list(),
  groups = NULL,
  snapshot = FALSE,
  verbose = TRUE,
  return_class = c("data.frame", "data.table")
)
}
\arguments{
\item{data}{Data frame of GPS rows.}

\item{steps}{Steps to apply. Any of: \code{"duplicates"}, \code{"errors"},
\code{"speed_fixed"}, \code{"speed_stat"}, \code{"spatial"}, \code{"denoise"}.}

\item{action}{Either \code{"drop"} to remove rows in row-filtering steps or
\code{"flag"} to keep rows and add step-specific flag columns.}

\item{paddocks_sf}{Optional paddock polygons (\code{sf}) required for \code{"spatial"}.}

\item{max_speed_mps}{Fixed speed threshold (m/s).}

\item{speed_stat_method}{Statistical speed method.}

\item{window_start}{Optional global deployment start datetime.}

\item{window_end}{Optional global deployment end datetime.}

\item{deployment_windows}{Optional deployment window table passed to
\code{gps_clean_errors()}.}

\item{deployment_groups}{Optional join columns for \code{deployment_windows}.}

\item{buffer_m}{Paddock buffer in meters.}

\item{append_paddock}{Logical; append paddock name column.}

\item{paddock_col}{Output paddock column name.}

\item{denoise_method}{Denoise method passed to \code{gps_denoise()}.}

\item{denoise_state_col}{Optional state column for state-aware denoise.}

\item{denoise_inactive_states}{Inactive state labels for state-aware denoise.}

\item{denoise_keep_raw_coords}{Logical; keep \code{lon_raw} and \code{lat_raw}.}

\item{step_args}{Optional named list of step-specific arguments. Names must
match requested steps.}

\item{groups}{Grouping columns for speed/denoise/modal paddock operations.}

\item{snapshot}{Logical; print snapshots after each step.}

\item{verbose}{Logical; print details.}

\item{return_class}{Output class: \code{"data.frame"} (default) or \code{"data.table"}.}
}
\value{
Cleaned GPS data with combined cleaning audit attributes.
}
\description{
Applies selected cleaning steps and returns cleaned data. Each step attaches
an audit summary, and \code{gps_clean()} combines those summaries into the final
\code{cleaning_summary} attribute.
}
