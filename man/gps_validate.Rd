% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gps_validation.R
\name{gps_validate}
\alias{gps_validate}
\title{Validate GPS data}
\usage{
gps_validate(
  data,
  drop_invalid = FALSE,
  large_gap_mins = 60,
  groups = NULL,
  check_zero_zero = FALSE,
  return_class = c("data.frame", "data.table")
)
}
\arguments{
\item{data}{Input data frame with standard GPS columns.}

\item{drop_invalid}{Logical; if \code{TRUE}, rows with invalid required fields
are removed from returned \code{data}.}

\item{large_gap_mins}{Positive number. Intervals longer than this are counted
as large gaps.}

\item{groups}{Optional grouping columns used for ordering, duplicates, and
interval checks. Defaults to \code{deployment_id} and \code{sensor_id} when
\code{deployment_id} is present, otherwise \code{sensor_id}.}

\item{check_zero_zero}{Logical; flag \verb{(0, 0)} coordinates as invalid.}

\item{return_class}{Output class for returned tables: \code{"data.frame"}
(default) or \code{"data.table"}.}
}
\value{
A list with class \code{grz_validation} containing the validation status,
message, typed data when valid, and compatibility tables for older
workflows.
}
\description{
Checks that the required GPS columns are present and that the core fields are
parseable. Row-level data quality checks are handled by \code{gps_qc_summary()}
and cleaning functions.
}
