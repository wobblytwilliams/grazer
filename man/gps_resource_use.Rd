% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gps_spatial_resource.R
\name{gps_resource_use}
\alias{gps_resource_use}
\title{Summarise GPS use near resources}
\usage{
gps_resource_use(
  data,
  resources,
  radius_m = 25,
  resource_id_col = NULL,
  resource_type_col = NULL,
  resource_crs = NULL,
  epoch = c("day", "hour", "week", "month", "interval"),
  epoch_mins = NULL,
  groups = NULL,
  metric_crs = NULL,
  verbose = TRUE,
  return_class = c("data.frame", "data.table")
)
}
\arguments{
\item{data}{Data frame with \code{sensor_id}, \code{datetime}, \code{lon}, \code{lat}.}

\item{resources}{\code{sf} object containing point, line, or polygon resources.}

\item{radius_m}{Distance threshold in metres for near-resource use.}

\item{resource_id_col}{Column in \code{resources} used as the resource identifier.
If \code{NULL}, row numbers are used.}

\item{resource_type_col}{Optional column in \code{resources} describing resource
type, such as water, shade, or supplement.}

\item{resource_crs}{CRS to assign when \code{resources} has no CRS.}

\item{epoch}{Epoch level: \code{"day"} (default), \code{"hour"}, \code{"week"}, \code{"month"},
or \code{"interval"}.}

\item{epoch_mins}{Positive epoch duration in minutes. Supplying this uses
fixed-duration \code{"interval"} epochs anchored to Unix time in UTC.}

\item{groups}{Grouping columns for summaries. Defaults to \code{deployment_id}
and \code{sensor_id} when \code{deployment_id} is present, otherwise \code{sensor_id}.}

\item{metric_crs}{Projected CRS used for area calculations. \code{NULL} selects a
UTM CRS from the GPS coordinates.}

\item{verbose}{Logical; print a short summary.}

\item{return_class}{Output class for tabular output: \code{"data.frame"}
(default) or \code{"data.table"}. Ignored when \code{return_geometry = TRUE}.}
}
\value{
Resource-use summary table by stream, epoch, and resource.
}
\description{
Summarises fixes assigned to their nearest resource and counts fixes within
\code{radius_m}. Use \code{radius_m = 0} for polygon zones where only fixes inside or
touching the resource should count as near.
}
