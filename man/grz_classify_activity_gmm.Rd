% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grz_behavior.R
\name{grz_classify_activity_gmm}
\alias{grz_classify_activity_gmm}
\title{Classify Active/Inactive States Using GMM}
\usage{
grz_classify_activity_gmm(
  data,
  groups = NULL,
  step_col = "step_m",
  turn_col = "turn_rad",
  feature_set = c("adaptive", "legacy"),
  adaptive_window_mins = "auto",
  adaptive_window_mult = 4,
  adaptive_window_min_mins = 30,
  state_col = "activity_state_gmm",
  component_col = "activity_component_gmm",
  inactive_prob_col = "inactive_prob_gmm",
  fit_max_rows = 200000L,
  max_iter = 200L,
  tol = 1e-05,
  min_var = 1e-06,
  smoothing = c("none", "median", "hmm"),
  median_window_n = 5L,
  hmm_self_transition = 0.98,
  seed = 1,
  verbose = TRUE,
  return_class = c("data.frame", "data.table")
)
}
\arguments{
\item{data}{Input GPS data.}

\item{groups}{Grouping columns used for track-wise decoding.}

\item{step_col}{Step-distance column (meters).}

\item{turn_col}{Turn-angle column (radians).}

\item{feature_set}{GMM feature set. \code{"adaptive"} (default) augments step and
turn with adaptive-window displacement features. \code{"legacy"} uses only
step and turn.}

\item{adaptive_window_mins}{Adaptive feature window size in minutes. Use
\code{"auto"} (default) to scale window length from each track's sampling
interval.}

\item{adaptive_window_mult}{Multiplier applied to base sampling interval when
\code{adaptive_window_mins = "auto"}.}

\item{adaptive_window_min_mins}{Lower bound for auto window size (minutes).}

\item{state_col}{Output state column name.}

\item{component_col}{Output numeric component id column.}

\item{inactive_prob_col}{Output posterior probability column for inactive
state.}

\item{fit_max_rows}{Maximum rows used to fit the GMM (sampled if needed).}

\item{max_iter}{Maximum EM iterations.}

\item{tol}{EM convergence tolerance on log-likelihood.}

\item{min_var}{Minimum per-component feature variance for numerical
stability.}

\item{smoothing}{Posterior smoothing method: \code{"none"}, \code{"median"}, or
\code{"hmm"}.}

\item{median_window_n}{Window size (number of rows) for median smoothing when
\code{smoothing = "median"}.}

\item{hmm_self_transition}{Self-transition probability used when
\code{smoothing = "hmm"}.}

\item{seed}{Random seed for reproducible fitting/subsampling.}

\item{verbose}{Logical; print summary output.}

\item{return_class}{Output class: \code{"data.frame"} (default) or \code{"data.table"}.}
}
\value{
Input data with appended GMM activity columns.
}
\description{
Fits a 2-component Gaussian Mixture Model (GMM) on transformed movement
features and decodes each row into \code{inactive}/\code{active} states using posterior
probabilities. Optional median or HMM smoothing can be applied to reduce label
flicker.
}
