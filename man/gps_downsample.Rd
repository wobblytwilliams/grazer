% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gps_alignment.R
\name{gps_downsample}
\alias{gps_downsample}
\title{Downsample GPS fixes to a lower frequency}
\usage{
gps_downsample(
  data,
  target_mins,
  method = c("representative", "rigid"),
  phase_mins = 0,
  groups = NULL,
  verbose = TRUE,
  return_class = c("data.frame", "data.table")
)
}
\arguments{
\item{data}{Data frame with \code{sensor_id}, \code{datetime}, \code{lon}, and \code{lat}.}

\item{target_mins}{Target interval in minutes.}

\item{method}{Downsample mode: \code{"representative"} or \code{"rigid"}.}

\item{phase_mins}{Offset from the first timestamp in each group before the
first target time is created.}

\item{groups}{Grouping columns for independent streams. Defaults to available
\code{deployment_id}, \code{animal_id}, and \code{sensor_id}.}

\item{verbose}{Logical; print a short summary.}

\item{return_class}{Output class: \code{"data.frame"} (default) or \code{"data.table"}.}
}
\value{
Downsampled GPS data. A \code{gps_reg} attribute summarises input
fixes, retained fixes, gaps, and achieved sampling interval.
}
\description{
Selects one observed fix per target time within each animal or sensor stream.
\code{method = "rigid"} keeps the first fix in each target interval, while
\code{method = "representative"} keeps the fix closest to each target time.
}
