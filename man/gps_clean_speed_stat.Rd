% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gps_cleaning.R
\name{gps_clean_speed_stat}
\alias{gps_clean_speed_stat}
\title{Clean GPS speed outliers using a data-driven threshold}
\usage{
gps_clean_speed_stat(
  data,
  method = c("mad", "quantile"),
  k = 4,
  prob = 0.995,
  min_threshold_mps = 4,
  groups = NULL,
  action = c("drop", "flag"),
  flag_col = "is_speed_outlier",
  keep_speed_cols = FALSE,
  verbose = TRUE,
  snapshot = FALSE,
  return_class = c("data.frame", "data.table")
)
}
\arguments{
\item{data}{Data frame of GPS rows.}

\item{method}{Threshold method: \code{"mad"} or \code{"quantile"}. The MAD method is
fitted to the upper quartile of positive \code{log1p(speed_mps)} values so resting
and near-resting fixes do not dominate the threshold.}

\item{k}{MAD multiplier (used when \code{method = "mad"}).}

\item{prob}{Quantile probability (used when \code{method = "quantile"}).}

\item{min_threshold_mps}{Lower bound for threshold.}

\item{groups}{Grouping columns for step/speed calculation.}

\item{action}{Either \code{"drop"} to remove rows above the speed threshold or
\code{"flag"} to keep all rows and add a logical flag column.}

\item{flag_col}{Name of the speed flag column when \code{action = "flag"}.}

\item{keep_speed_cols}{Keep \code{step_dt_s}, \code{step_m}, and \code{speed_mps} columns.}

\item{verbose}{Logical; print drop counts.}

\item{snapshot}{Logical; print quick snapshot after step.}

\item{return_class}{Output class: \code{"data.frame"} (default) or \code{"data.table"}.}
}
\value{
Cleaned GPS data with cleaning audit attributes.
}
\description{
Clean GPS speed outliers using a data-driven threshold
}
