% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gps_social.R
\name{gps_social}
\alias{gps_social}
\title{Calculate standard GPS social proximity metrics}
\usage{
gps_social(
  data,
  thresholds_m = c(25, 30, 50, 100),
  herd_groups = NULL,
  interpolate = FALSE,
  align_interval_mins = "base",
  verbose = TRUE,
  return_class = c("data.frame", "data.table")
)
}
\arguments{
\item{data}{Data frame with \code{sensor_id}, \code{datetime}, \code{lon}, and \code{lat}.}

\item{thresholds_m}{Distance thresholds in metres.}

\item{herd_groups}{Herd partition columns. Defaults to available
\code{deployment_id} and \code{paddock}, so animals are only compared within those
partitions.}

\item{interpolate}{Logical; if \code{TRUE}, regularise and interpolate fixes
before comparing animals.}

\item{align_interval_mins}{Alignment interval in minutes, or \code{"base"} to use
the median observed interval.}

\item{verbose}{Logical; print a short summary.}

\item{return_class}{Output class: \code{"data.frame"} (default) or \code{"data.table"}.}
}
\value{
Input rows with nearest-neighbour, mean-distance, group-size, and
threshold count metrics.
}
\description{
Convenience wrapper around nearest-neighbour distance and neighbour counts.
The output remains row-level so it can be summarised by animal, sensor,
datetime, or user-defined epochs. Use \code{gps_proximity()} when pair-level
distances are required and \code{gps_contacts()} when association events are
required.
}
