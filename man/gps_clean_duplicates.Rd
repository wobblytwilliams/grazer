% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gps_cleaning.R
\name{gps_clean_duplicates}
\alias{gps_clean_duplicates}
\title{Drop or flag duplicate GPS fixes}
\usage{
gps_clean_duplicates(
  data,
  keys = c("sensor_id", "datetime", "lon", "lat"),
  action = c("drop", "flag"),
  flag_col = "is_duplicate_fix",
  verbose = TRUE,
  snapshot = FALSE,
  return_class = c("data.frame", "data.table")
)
}
\arguments{
\item{data}{Data frame of GPS rows.}

\item{keys}{Columns used to identify duplicates.}

\item{action}{Either \code{"drop"} to remove duplicate rows or \code{"flag"} to keep
all rows and add a logical flag column.}

\item{flag_col}{Name of the duplicate flag column when \code{action = "flag"}.}

\item{verbose}{Logical; print drop or flag counts.}

\item{snapshot}{Logical; print quick snapshot after step.}

\item{return_class}{Output class: \code{"data.frame"} (default) or \code{"data.table"}.}
}
\value{
Cleaned GPS data. Attributes \code{cleaning_summary}, \code{removed_rows}, and
\code{flagged_rows} contain audit information where relevant.
}
\description{
Identifies duplicate fixes from user-selected key columns. By default the
first matching row is retained and later duplicate rows are removed.
}
