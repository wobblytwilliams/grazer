% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grz_behavior.R
\name{grz_tune_thresholds}
\alias{grz_tune_thresholds}
\title{Tune idle/graze thresholds with clearer diagnostics}
\usage{
grz_tune_thresholds(
  data,
  groups = NULL,
  cohort_col = NULL,
  tz_local = "UTC",
  step_col = "step_m",
  speed_col = "speed_mps",
  turn_col = "turn_rad",
  rest_step_grid = seq(3, 12, by = 1),
  rest_speed_grid = seq(0.03, 0.09, by = 0.02),
  graze_speed_max = 0.6,
  travel_speed_min = 0.6,
  travel_turn_max = 0.6,
  min_run_n = 2L,
  short_bout_mins = 10,
  night_hours = c(20:23, 0:5),
  max_rows = 25000L,
  max_points_plot = 150000L,
  seed = 1,
  verbose = TRUE,
  return_class = c("data.frame", "data.table")
)
}
\arguments{
\item{data}{Input data containing GPS rows.}

\item{groups}{Group columns used for transitions and bout calculations.}

\item{cohort_col}{Optional cohort column for faceting.}

\item{tz_local}{Time zone used to derive hour-of-day.}

\item{step_col}{Step-distance column (m).}

\item{speed_col}{Speed column (m/s).}

\item{turn_col}{Turn-angle column (radians).}

\item{rest_step_grid}{Candidate \code{rest_step_max} values for sweep.}

\item{rest_speed_grid}{Candidate \code{rest_speed_max} values for sweep.}

\item{graze_speed_max}{Passed into classification rule during sweep.}

\item{travel_speed_min}{Passed into classification rule during sweep.}

\item{travel_turn_max}{Passed into classification rule during sweep.}

\item{min_run_n}{Run smoothing threshold used during sweep.}

\item{short_bout_mins}{Bout duration threshold for instability metric.}

\item{night_hours}{Night hours used for night-rest metric.}

\item{max_rows}{Maximum rows used in threshold sweep (sampled if needed).}

\item{max_points_plot}{Maximum points used for density/CDF plots.}

\item{seed}{Random seed for reproducible subsampling.}

\item{verbose}{Logical; print sweep summary.}

\item{return_class}{Output class for returned tables.}
}
\value{
A list with \code{suggested}, \code{sweep}, \code{best}, \code{mixture}, and \code{plots}.
}
\description{
Builds practical threshold-tuning outputs for \code{rest} vs \code{graze} separation:
a data-driven starting cutoff from a two-component mixture on \code{log1p(step_m)},
a threshold sweep score surface, and visual diagnostics.
}
