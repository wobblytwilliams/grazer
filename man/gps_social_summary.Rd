% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gps_summary.R
\name{gps_social_summary}
\alias{gps_social_summary}
\title{Summarise GPS social metrics by epoch}
\usage{
gps_social_summary(
  data,
  epoch = c("day", "hour", "week", "month", "interval"),
  epoch_mins = NULL,
  groups = NULL,
  thresholds_m = c(25, 30, 50, 100),
  herd_groups = NULL,
  interpolate = FALSE,
  align_interval_mins = "base",
  verbose = TRUE,
  return_class = c("data.frame", "data.table")
)
}
\arguments{
\item{data}{Data frame with GPS rows or output from \code{gps_social()},
\code{gps_nearest_neighbour()}, or \code{gps_neighbours_within_range()}.}

\item{epoch}{Epoch level: \code{"day"} (default), \code{"hour"}, \code{"week"}, \code{"month"},
or \code{"interval"}.}

\item{epoch_mins}{Positive epoch duration in minutes. Supplying this uses
fixed-duration \code{"interval"} epochs anchored to Unix time in UTC.}

\item{groups}{Grouping columns for summaries. Defaults to available
\code{deployment_id}, \code{animal_id}, and \code{sensor_id}.}

\item{thresholds_m}{Distance thresholds used when social metrics must be
calculated from raw GPS rows.}

\item{herd_groups}{Herd partition columns passed to \code{gps_social()}.}

\item{interpolate}{Logical; passed to \code{gps_social()} when needed.}

\item{align_interval_mins}{Alignment interval passed to \code{gps_social()}.}

\item{verbose}{Logical; print a short summary.}

\item{return_class}{Output class: \code{"data.frame"} (default) or
\code{"data.table"}.}
}
\value{
Epoch social summary table with stable identifier, epoch, nearest
neighbour, group-size, and threshold columns.
}
\description{
Converts row-level social proximity metrics into one row per animal or
sensor and epoch. If social columns are not present, \code{gps_social()} is run
first using the supplied thresholds and alignment settings.
}
