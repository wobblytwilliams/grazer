% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gps_summary.R
\name{gps_epoch}
\alias{gps_epoch}
\title{Join GPS epoch summaries}
\usage{
gps_epoch(
  data = NULL,
  epoch = c("day", "hour", "week", "month", "interval"),
  epoch_mins = NULL,
  include = NULL,
  groups = NULL,
  movement = NULL,
  social = NULL,
  spatial = NULL,
  resource_use = NULL,
  thresholds_m = c(25, 30, 50, 100),
  herd_groups = NULL,
  interpolate = FALSE,
  align_interval_mins = "base",
  min_fixes = 5,
  metric_crs = NULL,
  verbose = TRUE,
  return_class = c("data.frame", "data.table")
)
}
\arguments{
\item{data}{Optional GPS data or a GPS-derived metric table.}

\item{epoch}{Epoch level: \code{"day"} (default), \code{"hour"}, \code{"week"}, \code{"month"},
or \code{"interval"}.}

\item{epoch_mins}{Positive epoch duration in minutes. Supplying this uses
fixed-duration \code{"interval"} epochs anchored to Unix time in UTC.}

\item{include}{Metric blocks to include. Values are \code{"movement"},
\code{"social"}, \code{"spatial"}, and \code{"resource_use"}. If \code{NULL}, blocks are
inferred from supplied tables and columns in \code{data}.}

\item{groups}{Grouping columns for summaries and joins.}

\item{movement}{Optional output from \code{gps_steps()} or
\code{gps_movement_summary()}.}

\item{social}{Optional output from \code{gps_social()} or \code{gps_social_summary()}.}

\item{spatial}{Optional output from \code{gps_spatial()}.}

\item{resource_use}{Optional output from \code{gps_resource_use()}.}

\item{thresholds_m}{Distance thresholds used when social metrics must be
calculated from raw GPS rows.}

\item{herd_groups}{Herd partition columns passed to \code{gps_social()}.}

\item{interpolate}{Logical; passed to \code{gps_social()} when needed.}

\item{align_interval_mins}{Alignment interval passed to \code{gps_social()}.}

\item{min_fixes}{Minimum number of fixes required before an MCP is computed.}

\item{metric_crs}{Projected CRS used for area calculations. \code{NULL} selects a
UTM CRS from the GPS coordinates.}

\item{verbose}{Logical; print a short summary.}

\item{return_class}{Output class: \code{"data.frame"} (default) or
\code{"data.table"}.}
}
\value{
Joined epoch summary table.
}
\description{
Builds or joins modelling-ready GPS epoch summaries. Raw GPS rows can be
summarised directly for movement and social metrics. Pre-computed outputs
from \code{gps_movement_summary()}, \code{gps_social_summary()}, \code{gps_spatial()}, and
\code{gps_resource_use()} can also be supplied and will be joined by their common
identifier and epoch columns.
}
